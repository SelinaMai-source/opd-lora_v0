## 结论：当前 v0 评测数据不是“每个 prompt 对应多个 targets”

在目前已经评测的 **InstrDialog 和 InstrDialog++ 的 processed 数据**中，每条样本都是：

```python
{
    "instruction": str,
    "input": str,
    "output": str
}
```

也就是：

\[
x=(\text{instruction},\text{input}),\qquad y^*=\text{output}
\]

每个完整输入 \(x\) 对应**一个 `output` 字符串**。仓库中的 `Example` 数据类将 `output` 定义为单个字符串，数据解析器也只读取单一的 `obj["output"]`。 

这里有两个容易混淆的地方。

### 1. `train50/eval10` 不是“每个 prompt 有 50 或 10 个 targets”

它表示每个任务段包含：

```text
50 条训练样本
10 条评测样本
```

例如某个任务段的结构是：

```python
Segment(
    segment_name="task1590_diplomacy_text_generation",
    train=[Example_1, ..., Example_50],
    eval=[Example_51, ..., Example_60],
)
```

每个 `Example_i` 自己只有一个 `output`。

### 2. 同一个 `instruction` 会对应很多 outputs，但它们不是同一个完整 prompt

一个任务段内的 50 个样本通常共享同一个任务说明，例如：

```text
根据前 5 条 Diplomacy 游戏消息，生成下一条消息。
```

但是它们的 `input` 不同：

```text
instruction 相同 + 对话 A → output A
instruction 相同 + 对话 B → output B
instruction 相同 + 对话 C → output C
```

模型实际看到的完整 prompt 是：

\[
\text{prompt}=\text{instruction}+\text{input}
\]

所以这仍然是不同的 prompts，而不是“同一个 prompt 有多个 targets”。

代码里有时出现复数变量 `targets`，只是因为一个 batch 中有多条样本：

```python
for ins, inp, tgt in zip(instructions, input_texts, targets):
    ...
```

这里每次循环中的 `tgt` 仍然只是一个字符串。

需要再区分一个概念：对于开放式对话生成，**语义上当然可能存在很多合理回复**；但当前数据集只标注了一条实际参考回复。因此，某个模型回答可能很合理，但只要不等于这一个参考字符串，strict EM 就仍然记为 0。你报告中的 token F1、ROUGE-L 和 BLEU，正是在一定程度上缓解 strict EM 过于苛刻的问题。:chatgpt-content-reference{index="6"}

---

# 一个来自当前 InstrDialog 数据的真实例子

在 `task1590_diplomacy_text_generation` 任务中，仓库里的一个真实样本是：

```json
{
  "instruction": "You are given first 5 messages from a series of message exchanges between 2 persons playing the game of Diplomacy... You need to generate the next message.",
  "input": [
    "Hi Kaiser, greetings from her majesty the queen... Seeing as you are my first choice in alliance...",
    "Hi Queen, greetings from Germany (the Kaserin, actually 😉 )",
    "You're my first choice for an alliance as well... Have you tried or seen that before?",
    "I haven't tried it or seen it",
    "Wait is Kaserin female for kaiser? Lol"
  ],
  "output": "Hahah yeah"
}
```

这里我对第一条长消息做了省略，但最后的参考输出确实是：

```text
Hahah yeah
```

也就是说，这一条样本只有一个标注 target。

定义：

```text
x = instruction + input
y* = "Hahah yeah"
```

下面分别看现有 SFT 和 OPSD 如何使用这一条数据。

---

# 一、现有 v0 SFT 如何使用这条样本

仓库当前的 formatting 会把 `instruction` 和 `input` 合成 user message，并额外加入简洁回答约束。

实际结构近似为：

```text
<|start_header_id|>user<|end_header_id|>

You are given first 5 messages from a series of message exchanges
between 2 persons playing the game of Diplomacy...
You need to generate the next message.

You must answer as concisely as possible without any explanations
or conversational fillers.

Input:
[
  "Hi Kaiser, greetings from her majesty the queen...",
  "Hi Queen, greetings from Germany (the Kaserin, actually 😉 )",
  "You're my first choice for an alliance as well...",
  "I haven't tried it or seen it",
  "Wait is Kaserin female for kaiser? Lol"
]

<|eot_id|>
<|start_header_id|>assistant<|end_header_id|>

Hahah yeah
<|eot_id|>
```

训练时：

- prompt 部分的 label 被 mask 为 `-100`；
- 只对 `"Hahah yeah"` 及终止符计算交叉熵；
- 模型始终在 gold prefix 上学习。

对应：

\[
\mathcal L_{\mathrm{SFT}}
=
-\sum_t\log p_\theta
\left(
y_t^*
\mid x,y_{<t}^*
\right)
\]

---

# 二、OPSD 中 Student 看什么

Student 不允许看到参考输出。

它的 prompt 与真实推理时完全一致：

```text
<|start_header_id|>user<|end_header_id|>

You are given first 5 messages from a series of message exchanges
between 2 persons playing the game of Diplomacy...
You need to generate the next message.

You must answer as concisely as possible without any explanations
or conversational fillers.

Input:
[
  "Hi Kaiser, greetings from her majesty the queen...",
  "Hi Queen, greetings from Germany (the Kaserin, actually 😉 )",
  "You're my first choice for an alliance as well...",
  "I haven't tried it or seen it",
  "Wait is Kaserin female for kaiser? Lol"
]

<|eot_id|>
<|start_header_id|>assistant<|end_header_id|>
```

即：

\[
P_S=x
\]

然后，由**当前正在训练的 student policy**采样生成回答。假设这一轮生成的是：

```text
Haha, yes.
```

记作：

\[
\hat y=\text{“Haha, yes.”}
\]

注意，这只是为了说明流程而假设的一条 student rollout，不是数据集中的第二个 target。

Student 完整 forward 序列成为：

```text
[Student prompt]
Haha, yes.
```

也就是：

\[
P_S\oplus\hat y
\]

---

# 三、OPSD 中 Teacher 看什么

Teacher 使用的是同一条数据，但额外看见训练期 privileged information：

```text
y* = "Hahah yeah"
```

由于这个任务是开放式对话生成，我建议不要告诉 teacher“这是唯一正确答案”，而应写成“这是一个有效参考回复”。

推荐 teacher prompt：

```text
<|start_header_id|>user<|end_header_id|>

You are given first 5 messages from a series of message exchanges
between 2 persons playing the game of Diplomacy...
You need to generate the next message.

You must answer as concisely as possible without any explanations
or conversational fillers.

Input:
[
  "Hi Kaiser, greetings from her majesty the queen...",
  "Hi Queen, greetings from Germany (the Kaserin, actually 😉 )",
  "You're my first choice for an alliance as well...",
  "I haven't tried it or seen it",
  "Wait is Kaserin female for kaiser? Lol"
]

<privileged_reference_output>
Hahah yeah
</privileged_reference_output>

The reference output above is one valid response rather than the
only acceptable wording. Use it to understand the intended meaning,
tone, and degree of brevity.

Now answer the original task. Do not quote, discuss, or mention the
reference output. Return only the requested response.

<|eot_id|>
<|start_header_id|>assistant<|end_header_id|>
```

即：

\[
P_T=x\oplus y^*\oplus\text{transition instruction}
\]

官方 OPSD 的数据构造也是：

- Student：只看原始 problem；
- Teacher：看 problem、reference solution 和重新解答的 transition prompt。

---

# 四、关键点：Teacher 不生成自己的另一条回答

这是 OPSD 最容易理解错的地方。

不是：

```text
Student 从 x 生成：
Haha, yes.

Teacher 从 x + y* 重新生成：
Hahah yeah

然后让 Student 学 Teacher 的回答
```

这更接近普通 sequence-level knowledge distillation。

真正的 OPSD 是：**Student 先生成一条 rollout，Teacher 和 Student 对同一条 rollout 进行打分。**

在上述例子中，Student rollout 是：

```text
Haha, yes.
```

因此两个完整 forward 输入分别是：

### Student forward

```text
[原始任务 x]
Haha, yes.
```

### Teacher forward

```text
[原始任务 x]
[参考输出 y* = "Hahah yeah"]
[transition instruction]
Haha, yes.
```

Teacher 看到的后半段仍然是 Student 生成的：

```text
Haha, yes.
```

而不是 teacher 自己重新生成的另一条文本。

官方实现也是先从 student prompt 生成 on-policy outputs，再将同一批 student token 分别接到 student prompt 和 teacher prompt 后面；随后从两个 prompt 各自的边界开始对齐 logits。

---

# 五、逐 token 看 Teacher 和 Student 的区别

假设 Student rollout 被分成：

```text
["Haha", ",", "yes", "."]
```

预测第一个 token 时：

\[
p_S(\cdot\mid x)
\]

对比：

\[
p_T(\cdot\mid x,y^*)
\]

Teacher 因为看到了：

```text
Hahah yeah
```

会更清楚这里应该：

- 用轻松、肯定的语气；
- 回复很短；
- 对 “Kaserin 是不是女性形式” 作肯定回应。

预测第三个 token `yes` 时，两者分别是：

\[
p_S(\cdot\mid x,\text{“Haha,”})
\]

和：

\[
p_T(\cdot\mid x,y^*,\text{“Haha,”})
\]

然后在每一个 student rollout token 位置上计算分布差异：

\[
\mathcal L_{\mathrm{OPSD}}
=
\frac{1}{T}
\sum_{t=1}^{T}
D\left[
p_T(\cdot\mid x,y^*,\hat y_{<t})
\;\middle\|\;
p_S(\cdot\mid x,\hat y_{<t})
\right]
\]

其中 \(D\) 可以使用 generalized JSD 或 KL。

这意味着 Teacher 教给 Student 的不是一个额外文本 target，而是：

```text
在 Student 自己已经生成到这个前缀时，
下一个 token 应该具有怎样的概率分布。
```

---

# 六、这和 SFT 的本质差异

以上面例子为例：

### SFT 学到的是

```text
在 gold prefix 上：

""          → Hahah
"Hahah"     → yeah
```

即：

\[
x,y^*_{<t}\rightarrow y_t^*
\]

### OPSD 学到的是

假设 Student 自己生成了：

```text
Haha, yes.
```

那么训练状态包括：

```text
""          → Teacher distribution
"Haha"      → Teacher distribution
"Haha,"     → Teacher distribution
"Haha, yes" → Teacher distribution
```

即：

\[
x,\hat y_{<t}\rightarrow p_T
\]

所以 OPSD 的主要价值不是“提供了更多 targets”，而是：

> **让模型在自己推理时真正可能访问到的前缀状态上接受监督。**

这正是 on-policy self-distillation 相比普通 teacher-forcing SFT 的核心差异。

---

# 七、用一张表概括这条数据的流向

| 项目 | Student | Teacher |
|---|---|---|
| 原始 instruction | 可见 | 可见 |
| 原始 input | 可见 | 可见 |
| 数据集 `output="Hahah yeah"` | **不可见** | **可见，作为 privileged reference** |
| rollout `"Haha, yes."` | Student 自己生成 | 使用同一条 rollout |
| 是否生成单独的 teacher answer | 否 | 否 |
| 梯度更新 | 是 | 否 |
| 测试时存在 | 是 | 否 |

因此，对于当前数据可以把 OPSD 的构造概括为：

```python
x = {
    "instruction": example.instruction,
    "input": example.input,
}

y_ref = example.output  # 单一参考输出

student_prompt = format(x)

teacher_prompt = format(
    x
    + one_valid_reference(y_ref)
    + answer_original_task_again_instruction
)

student_rollout = sample(student, student_prompt)

student_logits = forward(
    student_prompt + student_rollout
)

teacher_logits = forward_no_grad(
    teacher_prompt + student_rollout
)

loss = divergence(
    student_logits_on_rollout,
    teacher_logits_on_rollout,
)
```

最后要明确的是：**OPSD 的 Student 和 Teacher 并不是从两套不同的数据中取样。二者使用同一条 `(instruction, input, output)` 数据，区别只是 Student 不看 `output`，Teacher 把这个单一 `output` 当作训练期特权信息。**