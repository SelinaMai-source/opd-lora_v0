# 模型权重(15G,通过 modelscope 下载,不入库)
assets/

# Python
__pycache__/
*.pyc

# v1: adapter 权重 / 模型权重不入库;仅忽略仓库根级运行日志目录(results/logs 小日志保留)
final_adapter/
segment_adapters/
*.safetensors
/logs/

# v0 全量 pred/gold CSV（体积大，留数据盘不入库）
experiments_v1/v0_LoRA_SFT/predictions/
